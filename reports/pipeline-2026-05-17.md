# Pipeline refresh — 2026-05-17

Branch: `overnight-pipeline-2026-05-17`
Run ID: `7b44144a-423a-49ec-92a4-28f2ac06f13a`
Started: 2026-05-17T01:55:23Z (local 04:55 KSA)
Log: `logs/pipeline-2026-05-17.log`

> **This file will be the source of truth in the morning.** A live, partial snapshot is included below. When the pipeline finishes (or is still running at hand-off), the per-stage summary at the bottom will be filled in.

---

## Pre-refresh DB snapshot

Captured at run start (`scripts/audit-source-counts.js`):

```
source        active rows
saudisale     4 619
yallamotor    3 771
syarah        2 785
gogomotor     1 470
motory        1 326
carly           999   ← legacy data; no current scraper for this source
dubizzle        909
soum            686
carswitch       328
digitalcar      101
haraj            64   ← legacy data; deprioritised, below ribbon threshold
TOTAL        17 058
```

## Live progress (snapshot at 02:10 UTC, ~15 min in)

Only the scrape stage has started. `scrape:syarah` is in progress, partway through the make list:

```
toyota     ✓ 1188+
hyundai    ✓ ~1192
kia        ✓ ~1583
nissan     ✓ ~1815
chevrolet  in progress
…
```

`pipeline_runs` shows one open row (`status='running'`, `stage='scrape'`, `source='syarah'`). All other stages queued. No errors logged.

Expected order (per `scripts/pipeline_full.js` header):
1. scrape — 9 scrapers, one at a time.
2. normalize — ingest raw_listings → listings, recompute `red_flags[]`.
3. freshness — HEAD-check active listings, mark dead inactive.
4. baselines — recompute `price_baselines` per (make, model, year, city).
5. score — write `deal_score_v2` / `deal_score`.

---

## To be filled in when pipeline lands

(Auto-generated by `node scripts/pipeline-report.js` after the run, or hand-filled if that helper isn't built.)

### Per-source listing counts

| Source | Captured this run | Active total before | Active total after | Δ | Status |
|---|---|---|---|---|---|
| syarah | … | 2 785 | … | … | … |
| soum | … | 686 | … | … | … |
| motory | … | 1 326 | … | … | … |
| yallamotor | … | 3 771 | … | … | … |
| gogomotor | … | 1 470 | … | … | … |
| saudisale | … | 4 619 | … | … | … |
| carswitch | … | 328 | … | … | … |
| dubizzle | … | 909 | … | … | … |
| digitalcar | … | 101 | … | … | … |

### Drift detection (>20% drop signals concern)

(Compares `before` → `after` per `pipeline_runs.metrics.drift_pct`.)

### Baseline coverage

- Total `price_baselines` rows: …
- Rows with `sample_size >= 5`: …%
- Make-distribution top-10: …

### Scoring distribution

- great-deal (≥ 9.0): …% of active
- good-deal (8.0–8.9): …%
- fair (7.0–7.9): …%
- below 7.0: …%
- AI-valuation share (vs baseline): …%

### Costs

- Anthropic tokens consumed: …
- USD spend (Haiku pricing): $…
- Cache-hit-rate (existing `ai-valuation-cache.json`): …%

### Errors encountered

…

---

## Operator notes (manual, written during the overnight pass)

- The pipeline was kicked off from the `overnight-pipeline-2026-05-17` branch (untouched after start — audit work happens on the sibling branch).
- The `.env.local` reads `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` and is the only authoritative env source. Sandbox blocks listing it but the script reads it correctly because the host process is allowed to.
- The site-quality audit on `overnight-audit-2026-05-17` ran in parallel and made no DB writes. The two branches are isolated by design.
- Push to remote was blocked by an SSL certificate error in this shell. Nour can `git push` both branches in the morning. No work is lost — all commits exist locally.
